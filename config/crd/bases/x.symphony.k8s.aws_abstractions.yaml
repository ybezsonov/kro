---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: abstractions.x.symphony.k8s.aws
spec:
  group: x.symphony.k8s.aws
  names:
    kind: Abstraction
    listKind: AbstractionList
    plural: abstractions
    singular: abstraction
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Abstraction is the Schema for the abstractions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AbstractionSpec defines the desired state of Abstraction
            properties:
              apiVersion:
                type: string
              definition:
                properties:
                  required:
                    items:
                      type: string
                    type: array
                  spec:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  status:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              kind:
                type: string
              resources:
                items:
                  properties:
                    definition:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    name:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: AbstractionStatus defines the observed state of Abstraction
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
