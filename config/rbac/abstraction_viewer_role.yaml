# permissions for end users to view constructs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: construct-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: symphony
    app.kubernetes.io/part-of: symphony
    app.kubernetes.io/managed-by: kustomize
  name: construct-viewer-role
rules:
- apiGroups:
  - x.symphony.k8s.aws
  resources:
  - constructs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - x.symphony.k8s.aws
  resources:
  - constructs/status
  verbs:
  - get
